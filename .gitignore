node_modules/
dist/
*.local
.DS_Store
npm-debug.log*
.claude/worktrees/

# Generated by `node src/engine/cam/cam_check.mjs` — the NC it posts and the STL
# each program was cut from. Regenerated on demand and churns on every planner
# change, so tracking it makes a 5 MB diff out of a one-line feed tweak.
nc_program/
# Backplot SVGs and screenshots from the run-cam-web skill, used to confirm
# geometry without a browser. Working output, not source.
image_tool/
